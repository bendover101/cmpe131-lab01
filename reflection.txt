It allows different users to work on the same project with the security that each persons work is done with their own work instead of sharing like a google doc. This allows users to commit their changes into the main branch whenever they are done or feel like they've accomplished their goal. Problems it could prevent are preventing from another user to change your work. Having the original main branch there for reference if the user decides to backtrack on their work.
